# Core dependencies
rumps; sys_platform == "darwin"  # macOS menu bar library
requests
numpy
python-dateutil
python-dotenv

# AI/LLM backends
ollama
openai>=1.0

# Text-to-speech
pyttsx3
elevenlabs

# Speech recognition
faster-whisper
sounddevice

# Image processing (for blur regions)
pillow>=10.0

# Native macOS support (PyObjC) - macOS only
pyobjc-framework-Cocoa>=9.0; sys_platform == "darwin"
pyobjc-framework-Quartz>=9.0; sys_platform == "darwin"

# Python-Swift Bridge Server
flask>=3.0
flask-cors>=4.0
